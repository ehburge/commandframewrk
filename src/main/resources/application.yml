
org:
  slf4j:
  simpleLogger:
  defaultLogLevel: info

logging:
  level:
    co:
      ex: info
    org:
      springframework: ERROR
      apache:
        activemq:
          audit:
            base: ERROR
            message: WARN